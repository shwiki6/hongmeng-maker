# Android SDK Download

This repository is a download bridge for Android SDK files.

Run the workflow from **Actions > Download Latest Android SDK > Run workflow**.
The GitHub runner downloads the Google SDK tools and packages, then uploads one
`android-sdk-linux` artifact for download.

Included packages:

- `platform-tools`
- `platforms;android-36`
- `build-tools;36.0.0`
