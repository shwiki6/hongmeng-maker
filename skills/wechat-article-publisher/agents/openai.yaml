interface:
  display_name: "公众号文章发布"
  short_description: "Standalone WeChat article drafting with SVG decoration"
  default_prompt: "Use $wechat-article-publisher to write a polished WeChat article, generate inline SVG decoration, and create a reviewable draft."
