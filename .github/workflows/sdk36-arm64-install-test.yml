name: SDK 36 ARM64 install test

on: [push, workflow_dispatch]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    env:
      SDK_ROOT: ${{ github.workspace }}/sdk
      AAPT2_REPO: https://github.com/arriRgb31/aapt2-termux.git
      APKTOOL_VERSION: 2.11.1
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Check configuration
        run: |
          uname -a
          printf '%s\n' "$SDK_ROOT" "$AAPT2_REPO" "$APKTOOL_VERSION"
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            clang cmake ninja-build git curl unzip zip file zipalign \
            protobuf-compiler libprotobuf-dev libpng-dev libexpat1-dev \
            zlib1g-dev libc6-dev linux-libc-dev gcc g++ libstdc++-13-dev \
            libfmt-dev liblz4-dev libzstd-dev \
            android-libbase-dev android-libcutils-dev android-liblog-dev \
            android-libutils-dev android-libziparchive-dev
      - name: Download SDK 36 inputs
        run: |
          mkdir -p "$SDK_ROOT"
          curl -fL --retry 5 --retry-all-errors -o sdk-tools.zip \
            https://dl.google.com/android/repository/commandlinetools-linux-13114758_latest.zip
          unzip -q sdk-tools.zip -d "$SDK_ROOT"
          mkdir -p "$SDK_ROOT/cmdline-tools/latest"
          mv "$SDK_ROOT/cmdline-tools"/bin "$SDK_ROOT/cmdline-tools"/lib \
            "$SDK_ROOT/cmdline-tools"/NOTICE.txt "$SDK_ROOT/cmdline-tools"/source.properties \
            "$SDK_ROOT/cmdline-tools/latest/"
          yes | "$SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" --licenses >/dev/null || true
          "$SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" \
            "platform-tools" "platforms;android-36" "build-tools;36.0.0"
          test -s "$SDK_ROOT/platforms/android-36/android.jar"
      - name: Build native AAPT2
        run: bash scripts/build-native-aapt2.sh
      - name: Verify native AAPT2
        run: |
          set -eux
          export LD_LIBRARY_PATH="$(dirname "$(find /usr/lib /lib -name 'liblog.so.0*' -type f -o -name 'liblog.so.0*' -type l | head -n1)")${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
          "$GITHUB_WORKSPACE/package/aapt2" version
          "$GITHUB_WORKSPACE/package/aapt2" dump badging "$SDK_ROOT/platforms/android-36/android.jar" >/dev/null
