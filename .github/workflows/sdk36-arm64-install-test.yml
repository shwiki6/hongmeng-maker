name: SDK 36 ARM64 install test

on: [push, workflow_dispatch]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    env:
      SDK_ROOT: ${{ github.workspace }}/sdk
      AAPT2_REPO: https://github.com/arriRgb31/aapt2-termux.git
      APKTOOL_VERSION: 2.11.1
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Check configuration
        run: |
          uname -a
          printf '%s\n' "$SDK_ROOT" "$AAPT2_REPO" "$APKTOOL_VERSION"
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends clang cmake ninja-build git curl unzip zip file zipalign
